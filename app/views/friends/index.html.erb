<h1>Friends:</h1>

<% @friends.each do |friend| %>
<p><%= link_to friend.first_name, friend_path(friend) %> <%= link_to "Delete",
  friend_path(friend),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %></p>
<% end %>
