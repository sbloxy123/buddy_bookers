<h1>Bookings:</h1>

<% @bookings.each do |booking| %>
<p><%= link_to booking.description, booking_path(booking) %> <%= link_to "Delete",
  booking_path(booking),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %></p>
<% end %>
